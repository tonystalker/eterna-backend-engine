{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo \"DATABASE_URL: $DATABASE_URL\" && echo \"REDIS_URL: $REDIS_URL\" && npx prisma generate && npx prisma migrate deploy && node dist/core/bootstrap.js'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

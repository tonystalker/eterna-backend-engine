{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'export DATABASE_URL=${{Postgres.DATABASE_URL}} && export REDIS_URL=${{Redis.REDIS_URL}} && npx prisma generate && npx prisma migrate deploy && node dist/core/bootstrap.js'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
